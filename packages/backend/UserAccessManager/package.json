{
  "name": "user-access-manager",
  "version": "0.0.1",
  "description": "The service responsible for Authentication, Authorization, User Management and their information and Activity Log for each user",
  "author": "Pedro Henrique <sandbox0@hotmail.com>",
  "main": "src/index.ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "build:clean": "tsc --build --clean && rimraf build",
    "lint:tsc": "tsc -p tsconfig.json --noEmit",
    "test": "jest --colors --passWithNoTests"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  }
}
